# Data Folder

This folder stores local, non-committed runtime data.

## Files Created At Runtime

- `user_config.json`: Saved selected playlist IDs.
- `snapshots.jsonl`: Historical per-video snapshot rows.

These files are ignored by Git via `.gitignore`.
